import 'package:flutter/material.dart';

class SelectGameTime extends StatelessWidget {
  const SelectGameTime({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '09:08:33',
      style: TextStyle(
        fontFamily: 'Poppins',
        fontSize: 14,
        color: Colors.white,
        height: 1,
      ),
    );
  }
}
